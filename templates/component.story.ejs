import React from 'react'
import { storiesOf } from '@storybook/react'
import <%= props.name %> from '../<%= props.name %>'

storiesOf('<%= props.name %>').add('default', () => (
  <<%= props.name %> />
))
